<main>
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">

                <h1>Customer Project Request received</h1>

                {{#each allRequest}}
                <div class="card mt-3 mb-3 shadow-sm p-3 mb-5 bg-white rounded">

                    <p>Customer ID:{{this.userId}}</p>

                </div>
                {{/each}}
            </div>
        </div>
    </div>
</main>